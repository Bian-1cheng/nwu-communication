server:
  port: 8000
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fileupload?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  data:
    redis:
      host: 106.**.**.**
      password:
      port: 6379

mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

logging:
  level:
    com.dao: debug